spring:
  application:
    name: prices
  datasource:
    url: jdbc:h2:mem:prices-${random.uuid};DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  sql:
    init:
      mode: always

management:
  endpoints:
    web:
      exposure:
        include: health
